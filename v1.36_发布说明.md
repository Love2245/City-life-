# 都市生活 v1.36 · 全套源码 + 可玩单文件

> 交付时间：2026-08-29
> 本文件夹为完整源码工程（非仅单文件 HTML），可直接二次开发或双击游玩。

## 一、怎么玩（无需环境）
- 直接双击根目录 **`都市生活_v1.36.html`** → 浏览器打开即玩（约 194MB，已内联全部美术/音频资源）。
- 或进入 `dist-portable/index.html`（与根目录单文件内容相同）。

## 二、二次开发 / 本地构建
已自带 `node_modules`，无需重新安装依赖即可运行：

```bash
# 开发预览
npm run dev

# 类型检查 + 全量测试（里程碑交付强制项）
npm run check        # svelte-check
npm run test         # vitest 全量回归

# 两段式构建单文件 HTML
set NODE_OPTIONS=--max-old-space-size=8192
npm run build                     # 产出 dist/
python inline_single.py           # 把 dist/ 内联为 dist-portable/index.html
```

## 三、本版（v1.36）主要改动
1. **首页「🐱 喵喵对决」直进**：无携带猫自动领养橘猫，优先休闲赛、失败无惩罚。
2. **对决界面重做（参考杀戮尖塔）**：透明底 + 椭圆光晕立绘 + 实体卡 + 敌方意图条 + 能量球。
3. **明确结束回合**：底部「结束回合 ⏭」按钮，仅玩家阶段可点，敌方阶段自动置灰，不再卡死。
4. **牌堆可查 + 动画**：抽牌堆 / 弃牌堆可点击查看（mini-card 弹层），抽卡 / 回收带 fly 动画。
5. **猫咪名字 / 攻击 / 样貌全部取自数据**：对手 `desc` 透传展示，基础攻击名取自 `catSkills.json` 真实技能。
6. **删除剧情模式入口**：移除主菜单「剧情模式」按钮、`BackgroundView` 剧情 chip 及其 CSS、自动弹窗接线（底层 `gameState.story` 数据保留，核心主线 / 人情债卡显示不受影响）。

## 四、版本号（已 5 处同步）
- `src/version.ts` = `1.36`
- `package.json` = `1.3.6`
- `src-tauri/tauri.conf.json` × 2 = `1.3.6`
- `CHANGELOG.md` 已新增 v1.36 小节

## 五、自排查结论（交付前已验证）
- `svelte-check`：**0 error**（146 warning，均为历史 a11y / 未用 CSS）。
- `vitest`：**978 / 978 全过**。
- 修复的 6 个真实 bug（非猜测）：`activePetOf` 错从 `catBattle` 导入（实际在 `catCare`）；`MainMenuView` 残留调用已删的 `startStoryGame`；删 `StoryCardModal` 渲染导致 `uiState` 未用 + 人情债卡失效（已恢复）；`v134_battle_ui` 测试用「抓挠」子串计数受卡面描述干扰（改为按卡名精确计数）；`entry_smoke` 测试引用已删的剧情模式（已移除）；`tauri.conf.json` 第二处 `package.version` 未同步（已补）。

## 六、文件夹结构
```
都市生活_v1.36/
├─ 都市生活_v1.36.html      ← 双击即玩的单文件成品
├─ dist-portable/           ← 同上的构建产物
├─ src/                     ← Svelte 5 + TS 全部源码
├─ src-tauri/               ← Tauri 2 桌面壳配置
├─ tests/                   ← vitest 用例
├─ assets/ scripts/ docs/   ← 美术资源 / 构建脚本 / 文档
├─ node_modules/            ← 依赖（已包含，免安装）
├─ index.html / vite*.config.ts / svelte.config.js / tsconfig.json
├─ package.json / pnpm-lock.yaml / package-lock.json
├─ inline_single.py         ← 单文件内联脚本
├─ CHANGELOG.md / README.md / 各类更新说明
```
